import React, { FC } from "react";
import { Container, Icon, Text } from "./styled";

const Toast: FC = () => (
  <Container>
    <Icon />
    <Text>Message</Text>
  </Container>
);

export default Toast;
